---
name: generating-storyboard-gift-voiceover
description: Use when a user provides storyboard images, numbered shot grids, sequential video stills, or written shot descriptions for cultural, stationery, gift-box, or gifting products and asks for Chinese video voiceover, shot-matched captions, or scripts readable by an AI digital presenter. 适用于分镜图生成口播字幕；普通无镜头顺序的产品图分析不属于默认入口。
---

# 分镜图驱动的文创礼赠口播字幕

## 核心原则

沿用“可见事实 → 用户收益 → 真实痛点 → 合适场景 → 差异化口播 → 对抗检核”框架，将输入改为分镜序列，将输出改为与镜头对应、可直接交给 AI 数字人朗读的字幕文案。

一组分镜默认对应一条完整视频。先服从已有画面和节奏，再选择文案结构；不要把每格当成一个独立产品，也不要默认生成三至十条长篇口播。

## 输入处理

接受多宫格分镜图、编号分镜、按顺序上传的图片、视频关键帧或文字分镜。用户给出顺序、台词、时长、平台、受众、节日、语气或稿件数量时优先服从。

优先按明确编号、箭头、时间码识别镜头顺序。没有标记的多宫格暂按从左到右、从上到下；多图暂按上传顺序，并简述这一假设。只有歧义会改变关键动作或产品归属时才追问。局部文字不可辨认时标记未知，不猜品牌、参数或承诺；缺少全部画面且无文字描述时，请用户提供分镜。

区分实拍画面、手绘/AI 概念分镜和用户资料。概念分镜证明的是计划表现的画面，不能单独证明真实产品材质、组成、功能、授权等。实拍同框也不证明配件随附。价格、材质、尺寸、工艺、库存、销量等未确认信息不进入成稿。

## 工作流

1. 阅读 [分镜文案框架](references/framework.md)，拆分并编号镜头，建立“画面内容、证据来源、物件归属、动作、叙事作用、时长”的镜头卡与产品事实卡。
2. 把有证据的特点连接到收益、痛点和适用场景，选择一个主卖点，最多两个辅助卖点。分析可以丰富，成稿只保留镜头和时长能承载的内容。
3. 依据镜头顺序串成完整口播。镜头切换不必强制句号；一个句子可跨相邻镜头，但具体动作、部件和指示词必须对应当前画面。允许留白，不给每格硬塞话。
4. 已给时长时逐镜压缩；未给时长时提供明确标为“建议”的时长。依据框架中的估算规则检查总时长和局部拥挤程度，不把文字估算称为实际配音验证。
5. 生成逐镜表和一致的纯朗读稿。多版本仅在用户要求时生成，用“人物视角 × 主卖点 × 叙事结构”区分，仍遵守同一分镜约束。
6. 阅读 [对抗检核](references/adversarial-check.md)，检查事实、音画对应、节奏和数字人可读性，直接修正后交付。

## 输出契约

默认按顺序交付：

1. **简要判断**：镜头数量与顺序假设、关键事实边界、主卖点和适用场景、总时长是用户指定还是建议。
2. **分镜口播表**：镜号｜画面要点｜时长（已给/建议）｜口播字幕｜必要的剪辑提示。提示和朗读文字分列。
3. **数字人纯朗读稿**：独立纯文本代码块，只含最终要朗读的文字，以自然语义分行；不含标题、镜号、时间码、情绪标签、舞台指令、括号注释或 Markdown 标记。
4. **简短检核摘要**：只列实际发现并处理的问题、估算时长及仍影响发布的待确认项。没有问题时不机械列风险清单。

用户只要字幕/口播时，仅交付纯朗读稿，将镜头分析与检核留在内部；必要的时长假设放在代码块外。用户只要分析时不强行输出成稿。需要完整策划时再展开事实卡、卖点—收益—痛点表、场景匹配及多稿差异表。

用户要求 SRT/VTT 时按格式输出：有真实时间轴则对齐；只有分镜时长则标注为估算时间轴；无任何时长先给建议并标注。未试听配音，不声称逐字同步。纯朗读文本与带时间轴字幕分别交付。

## 快速判断与常见错误

| 情况 | 处理 |
|---|---|
| 多个镜头展示同一商品 | 聚合证据，串成一条视频 |
| AI 分镜画出了内部物件 | 只视为设计内容，未经确认不写成真实内含清单 |
| 同框出现咖啡、花、书或托盘 | 先判定为道具或归属未知，不写成赠品 |
| 镜头时长短、卖点太多 | 先删辅助卖点，不要求数字人倍速硬读 |
| 镜头没有信息增量 | 过渡、延续上一句或留白，不重复报幕 |
| 缺少收尾购买画面 | 可自然结束，不编造链接、折扣或抢购提醒 |
| 用户要求多稿 | 改叙事角度，不改产品事实，不默认重排镜头 |

避免照搬每条一百八十至二百六十字的长稿规则；避免将镜头说明念进配音；避免为了钩子或卖点私自增镜；避免写“我亲自用过”“买过都说好”等虚构经历。可以建议补拍或改序，但须与按现有分镜完成的可用稿分开。
